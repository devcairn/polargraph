# IMPORTANT: Replace all placeholder values before deploying to any environment.
# Generate a strong API key and base64-encode it:
#   echo -n "your-secret-key" | base64
apiVersion: v1
kind: Secret
metadata:
  name: polargraph-secrets
  namespace: polargraph
type: Opaque
data:
  # Placeholder: base64("changeme") — MUST be replaced before deploying
  api-key: Y2hhbmdlbWU=
